# Agent 任务助手：60关学习与验收说明

目标是能独立实现、解释和维护可靠的 Agent 系统。四章保持为1–20、21–40、41–52、53–60关；页面保留原课程标题、计划文件名与旧课程表。原练习代码位于 `060/001`，本轮不重写练习代码，也不预先创建项目成果。

## 从第一周开始的持续项目

第1关建立任务输入、只读工具、输出和测试的最小闭环。第2–7关逐步增加契约、检索、持久化、流式事件、计划和测试反馈。后续每关都在同一项目增加能力，原独立示例供参考。建议自行建立 `task-assistant/`，含实现、`tests/`、`evidence/` 和运行说明；不要把生成空文件当成完成。

从第1关积累固定任务集：任务输入、可验证预期、正常/失败分类。每次新增正常与失败用例，保存命令、环境、提交号、预期和实际输出。使用模拟工具先验证控制逻辑，真实模型结果另记，不混淆模拟通过与端到端成功。

## 四层证据与复习

1. 看懂示例：说明输入、状态、决策、工具边界，并预测失败行为。
2. 脱离答案实现：关掉参考，从需求实现最小能力，记录测试结果。
3. 改变条件仍能完成：完成页面给出的变式，保留失败与修复证据。
4. 隔几天独立复现：首次变式通过至少3天后，闭卷复建关键能力并测试。

页面要求证据文本与未来复习日期，但无法判断文字真实性、运行代码或证明工程能力。统计“有复现证据”表示曾提交四层自报证据；最近失败会单独显示，并保留历史。复习失败后记录原因、安排再次练习；不能把旧成功当成当前仍然掌握。建议复现后7天再复习，之后按遗忘情况调整。每天先处理到期复习，再做一项新增量，允许跨天。

## 阶段验收

第20、40、52、60关重跑阶段累计回归集。记录验收人、日期、代码版本、环境、运行命令、通过数/总数、失败清单与解释。提前定义本阶段必须满足的任务行为，失败则修复重测，未解决项明确标记不达标。页面不会自动替你签署验收。

第20关：单Agent闭环、错误退出、持久状态、成本记录及独立重建。

第40关：进程中断恢复、响应丢失后的幂等、超时取消、用户改口、部分失败必须有注入故障的证据。同一任务集、工具、模型版本、预算规则与运行环境对比单Agent和多Agent；预先固定重复次数，保存每次成功/失败、token/费用估算、总耗时，报告成功率、样本数、p50/p95及波动。把超时计作失败，费用包含重试和协调开销，明确定价来源/日期。根据结果选择单Agent、局部协作或多Agent，不预设赢家。

第52关：原生与框架实现同集回归，新环境可复建，故障边界与依赖版本可解释。

第60关：持续项目交付、陌生变式、闭卷复现、故障演示、未解决清单。第53关是需求收敛而非第一次开始实践；第58关默认仅本机容器验收，不自动发布公网。

## 数据兼容与备份

新状态使用 `agent-learning-v2`，含版本、逐关追加证据、下次复习日期与历史快照。旧页面的完成来源是代码内 `AUTO_DONE`（前7关），没有读取打卡localStorage；新版本将它保存为历史自报记录，注明来源含材料自动生成，不赋予任何学习层级。材料标记仍仅表示已准备。

`quiz-state`、各独立测验及未知localStorage键均不删除、不迁移覆盖。新状态格式异常时不覆盖原值，页面提示检查/导出。保存失败会提示，当前输入保留。导出按钮备份课程及键名含quiz/agent/checkin的记录。备份含证据文本，请自行保管；本轮未实现导入界面。浏览器存储按协议、主机、端口区分，请沿用 `http://localhost:8080`，不要换成127.0.0.1或file协议后误以为数据丢失。查询参数版本号不影响存储。

方法参考：[learn-hack](https://github.com/xdite/learn-hack) 的可见目标、目标倒推和重复变式练习。采用可操作练习设计，不采纳百倍提速等未经验证承诺。

## Agent 小镇：可玩首关

从课程顶部进入 `agent-town.html`。首关“天气机器人罢工”以原创地图、居民小葵和机器人滴答串联：接委托、回放错误路由、选择工具连接、配置城市来源与故障策略、运行4项本地测试，再处理两座新城市的迁移挑战。错误配置有执行轨迹、预期/实际和原因，可用渐进提示重试。完成后天气屏点亮，滴答加入工作室；其他三区明确为筹建中。

全部天气、故障和工具均为本地确定性模拟，没有真实模型/API调用，也没有代码沙箱。使用键盘Tab、Enter/空格及原生选择框即可操作；遵循系统减少动态效果偏好。建议用约10分钟尝试错误路径、解释原因并重玩，不强制等待10分钟。

游戏仅写 `agent-town-v1`，与课程学习证据和旧测验存储独立。每次配置变动都会取消当前测试通过状态，需重新测试；伙伴奖励用单一已发放标记保持幂等，重玩保留奖励，刷新恢复配置和阶段。损坏存档不覆盖，写入失败显示提示。课程“导出”按钮会包含键名含agent的游戏存档。

实现：`agent-town-engine.js`（纯模拟与状态转换）、`agent-town.js`（交互/存储）、`agent-town.css`（响应式地图界面）。验证命令：`node agent-town.test.cjs` 和 `node agent-curriculum.test.cjs`。

首关另有不计分的“先拆开一单”实验：装入城市写死示例后，可改变居民城市，一步步查看请求、路由、真正传入的city、调用和回复。每一步有中文解释，第二次运行对照上次输入与输出，并指出修改了哪些配置。这用于理解因果，不作为掌握证明。

## 小镇后续59关已补齐

四区域现均提供完整可玩任务，后续实现、逐关主题映射、存档规则与验证范围见 [Agent 小镇60关说明](agent-town-60-guide.md)。原先“其余三区筹建中”的首关说明仅记录首版范围，当前版本已扩展至全部60关。课程与游戏存储继续隔离；游戏不替代真实代码验收。
