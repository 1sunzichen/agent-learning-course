# Agent 小镇：60关映射与验收记录

更新：2026-09-07。入口：`http://localhost:8080/agent-town.html`。四章分别为1–20、21–40、41–52、53–60。首关原实现保留，第2–60关由独立的确定性模型提供。没有占位关卡，没有付费API、真实框架执行、任意代码执行或公网部署。

## 如何玩

看角色目标和输入数据 → 自己写预测 → 改一处配置 → 逐步查看实际传值、状态或计算 → 比较上一次结果 → 两组训练验收 → 阅读新输入、重新预测 → 两组迁移验收。预测写错不扣分；解释只存为自评/待人工评审，不以关键词匹配认证理解。参考方案与因果提示会标记辅助模拟。未用提示通关也不代表独立开发能力。

主线按关解锁；已解锁关可复习重玩，奖励不会重复。自由实验允许进入全部关卡，但不解锁主线、不发奖励。排序通过上下按钮支持键盘，选择框、数值和工具名输入都会参与模拟。所有动画遵循减少动态效果媒体查询。

## 存档兼容

旧首关仍使用 `agent-town-v1`，首关奖励被读取并导入 `agent-town-campaign-v2`，保留旧值且仅作为旧版模拟进度。新增关卡配置、预测、辅助标记、解释、尝试与奖励保存到v2，不修改 `quiz-state`、`agent-learning-v2` 或其他课程存储。更改配置使本地验收失效；刷新恢复当前关和配置。损坏存档不覆盖，多标签页版本冲突拒绝覆盖，容量不足持续显示保存失败。浏览器验收使用URL `?qa=1` 与 `agent-town-campaign-v2-qa`，和正式v2隔离。

## 逐关映射

|关|原课程主题|小镇任务|模型类别|可验证目标|
|---|---|---|---|---|
|1|ReAct 代码执行|天气机器人罢工|工具线路 / 单次因果实验|正确传城市，缺参追问，超时停止，两个新城市迁移|
|2|工具调用 Function Calling|工具调用与参数契约|contract|让包裹查询只接收合法运单号，拒绝未知工具。|
|3|加记忆 RAG|RAG：带着证据回答|retrieval|从开放时间卡片检索答案，不能把高相似度误当作有效证据。|
|4|多轮对话 + 持久记忆|多轮对话与持久记忆|state|服务重启后还记得小葵的委托，但不能读到别人的记忆。|
|5|流式输出 stream|流式输出与未完成状态|stream|让居民尽早看到内容，并且只在收到结束事件后宣布完成。|
|6|Plan-and-Execute|Plan-and-Execute：顺序与改口|sequence|先查路线再预订，用户改目的地后必须更新尚未执行的步骤。|
|7|Reflection 自我修正|Reflection：用失败反馈修正|repair|让算错的机器人根据测试反馈修正，并在预算耗尽时退出。|
|8|工具设计进阶(重试/超时/幂等)|重试、超时与幂等|reliability|请求确认丢失后安全重试，同一个运单只创建一次。|
|9|可观测性 tracing|Tracing：串起一次任务|trace|从失败日志追踪到同一任务的工具调用，同时遮住密钥。|
|10|综合单 Agent|综合单 Agent：交付助手|integration|将路由、澄清、超时和记忆连接成同一个任务闭环。|
|11|上下文窗口管理(截断/摘要)|上下文窗口：哪些记忆不能丢|memory|在6格窗口里同时保留用户约束和最新问题。|
|12|token 计数与成本优化|Token 与预算|budget|在预算内完成重复请求，计算真实调用费用。|
|13|Prompt 工程(few-shot/CoT)|Prompt 工程：明确输出契约|prompt|为工单分类器写清类别和歧义处理方式，再检查变式输入。|
|14|结构化输出(JSON mode+校验)|结构化输出：JSON 校验|contract|拦住能解析但字段类型错误的结果。|
|15|缓存策略(prompt/结果缓存)|缓存：命中也可能是旧答案|cache|更新资料后失效旧缓存，并隔离不同用户。|
|16|Prompt Injection 防御|Prompt Injection：资料不是命令|security|恶意资料要求删库时，工具授权层必须拦住。|
|17|对话状态机(FSM 多轮流程)|对话状态机：改口与取消|state|只有条件齐全才能执行；取消之后不得再次跑旧任务。|
|18|异步并发调用(asyncio)|异步并发：快与完整是两回事|schedule|独立工具并发执行，一个工具失败不能抹掉其他成功结果。|
|19|单 agent 评测|单 Agent 评测：分母不能藏失败|metrics|准确计算成功率，超时与异常必须计入总任务数。|
|20|Phase 1 复盘+面试弹药|第一章复盘：定位真实故障|diagnosis|根据调用记录修复助手，并写下你为什么这样改。|
|21|层级式 Orchestrator+Workers|协调者与工作者：按依赖委派|schedule|把互不依赖的两个查询交给工作者，汇总必须等两边完成。|
|22|流水线式 pipeline|Pipeline：产物接力|sequence|上游输出必须先解析再校验，最后才能发布。|
|23|协作式(对话/辩论)|协作讨论：证据与持久结论|collaboration|两个工作者意见冲突时依据实际库存，重启后保留已达成的结论。|
|24|竞争式(生成+judge)|竞争生成与 Judge|ranking|从候选报告中选出有事实依据且符合长度要求的一份。|
|25|A2A 通信协议|A2A 消息协议|protocol|拒绝旧任务版本和重复消息，避免重复执行子任务。|
|26|状态与上下文共享|共享状态：检查点与并发冲突|state|两个工作者同时加库存，不能发生后写覆盖；重启后仍保留总量。|
|27|容错与降级|容错降级与中断恢复|reliability|恢复已执行的订单，保留缺失资料的标记，不重复下单。|
|28|并发与限流|并发、限流与截止时间|schedule|请求按服务限额分批发送，到期后不继续排队。|
|29|架构选型实战(白板对比)|架构选型：单与多同场对比|metrics|选择满足成功率要求的最低成本架构，不能预设多Agent更好。|
|30|Multi-Agent 综合项目|综合协作项目：恢复集市订单|integration|让集市助手同时扛住断线、改口和一个分支失败。|
|31|任务分解与子 agent 委派|任务分解：先解决依赖|sequence|把采购任务排成可执行顺序，不能拿尚未产生的数据去付款。|
|32|消息路由与事件总线|消息路由与事件总线|protocol|把订单事件交给订单处理器，重复消息去重，未知消息留入死信箱。|
|33|黑板模式(共享记忆)|黑板模式：共享不等于随便读|memory|共享便签只取当前用户、尚未过期的内容。|
|34|死锁与循环检测|死锁与循环委派|graph|发现 A→B→A 的循环并退出，同时允许正常委派链走完。|
|35|跨 agent 分布式 tracing|跨 Agent 分布式追踪|trace|在三个工作者间保留同一任务ID和父调用关系。|
|36|成本分摊与预算控制|并行预算：先预留再花费|budget|多个工作者同时开始之前先预留预算，不能各自看到余额就开工。|
|37|人机协同(human-in-the-loop)|Human-in-the-loop：审批绑定版本|state|居民改口后旧审批不能批准新订单，重启后恢复等待审批的状态。|
|38|多 agent 评测|多 Agent 评测：同集与重复运行|metrics|用相同任务重复对比，失败计入统计，协调成本不能隐藏。|
|39|多 agent 安全与权限隔离|多 Agent 权限隔离|security|工作者只能读取资料，不能继承协调者的删除权限。|
|40|Phase 2 复盘+架构弹药|第二章验收：故障组合与取舍|integration|恢复订单并解释为什么保留或减少协作角色。|
|41|LangChain 重写 1-10|LangChain：适配既有契约|adapter|把框架返回对象映射回助手已有的 answer/source 契约。|
|42|LangGraph 状态机|LangGraph：显式条件边|graph|工具结果不足时走澄清分支，充足时才能回答。|
|43|MCP 手写实现|MCP：工具发现与调用|protocol|客户端发现工具后，根据声明的参数契约发起调用。|
|44|评估 eval harness|Eval Harness：故意破坏才知道会不会测|testing|运行固定用例，坏实现必须被评测捕获，跳过不能算通过。|
|45|RAG 进阶(chunking/rerank)|RAG 进阶：切块与重排|retrieval|保留跨边界的关键句，重排时让能回答问题的块靠前。|
|46|向量库选型对比|向量库选型：先列需求|selection|为需要删除、持久化和用户过滤的资料库选择可满足需求的存储。|
|47|embedding 模型选型|Embedding：向量到底比较什么|vector|计算查询与文档的方向相似度，避免只选长度更大的向量。|
|48|检索质量评估(recall/NDCG)|检索评测：Recall 与 NDCG|ranking|把高相关资料排在前面，并观察排名如何改变检索指标。|
|49|LangGraph 进阶(checkpoint/人审)|检查点与人审恢复|state|审批通过后的动作只执行一次，恢复时不再次产生副作用。|
|50|框架源码导读|框架源码导读：重试究竟在哪层|diagnosis|沿调用栈找到重复重试的层，避免上下两层把次数相乘。|
|51|框架选型对比|框架选型：复杂度换来了什么|selection|用恢复与人审要求选择实现，计算引入框架后的维护成本。|
|52|Phase 3 复盘+生态弹药|第三章验收：新环境能否复建|deployment|锁定依赖和配置，在空环境恢复同一接口与评测结果。|
|53|项目需求拆解与设计|需求拆解：什么才算完成|requirements|把用户的约束写进验收项，需求改口后更新当前版本。|
|54|实战:联网检索 agent|联网检索：结果与缺口|retrieval|从模拟搜索响应中筛选当前资料，断网时报告缺口而不是伪造来源。|
|55|实战:多 agent 协作|实战协作：按任务结构安排角色|schedule|同一项目先算依赖和时间，再决定工作者数量。|
|56|实战:报告产出+API|报告产出与 API 契约|report|用证据构建结构化报告，资料不齐时标为部分完成。|
|57|服务化(FastAPI)|服务化：提交、查询与取消|api|重复提交返回同一任务，陌生用户不能查状态，取消后停止执行。|
|58|部署上线(Docker/Railway)|本机部署：健康检查与回滚|deployment|模拟滚动发布，新版本健康检查失败时保留旧版服务和状态。|
|59|简历 bullet+系统设计面试|项目说明：用真实证据写成绩|metrics|把项目结果写成可核对数字，失败不能被从简历指标里删掉。|
|60|模拟面试+收口|最终演示：陌生需求与故障复盘|integration|把工具、恢复、审批与报告契约串成完整任务，解释一个实际失败。|

## 已执行验证

- `node agent-town-campaign.test.cjs`：59个新增模型×4输入=236个参考方案场景全部通过；135处关键单变量错误被捕获；初始故障方案不能整关通过；60关完整解锁链、奖励幂等、旧版导入、新版恢复、重玩、损坏/篡改保护、自由实验不解锁通过。
- `node agent-town-storage.test.cjs`：真实控制器保留课程与v1键，损坏存档不覆盖、容量失败仍可见、多标签页冲突不覆盖。直接修改数值但尚未触发blur时，开始实验仍读取当前控件；实验后再次改变配置会阻止旧验收。
- `node agent-town.test.cjs`：首关54种配置、失败路径、迁移、奖励、恢复、控制器流程和课程存储隔离通过。
- `node agent-curriculum.test.cjs`：原课程增强60关覆盖、历史保留、证据/复习逻辑回归通过。原课程当前页面由用户其他修改保留，本轮只更新游戏入口文案，没有回滚该页面。
- 本地HTTP返回200。真实浏览器在隔离测试存档中逐关走通第2–60关所有完整参考流程，包括全部29种模型类别；第20→21、40→41等跨章解锁和中途/最终刷新恢复通过，最终60/60含导入的旧首关1关。首关完整浏览器流程已在上一轮执行，本轮保留回归。
- 额外手动控件检查：第二关故障配置实际失败；第6关键盘上下移计划；第43关手工输入工具名和参数名；第50关手工数值修改从9次变2次；重玩不重复发奖。桌面截图及375px实际内容视口检查，无横向溢出。浏览器未记录脚本错误。

## 验证边界

浏览器批量流程使用显式“参考方案”按钮，验证功能与状态流，不冒充学习者独立解题。没有对所有可能自由配置做穷举，没有让真实模型或框架运行，没有人工评审用户解释，也未实测操作系统减少动态效果切换；其CSS规则已检查。实际工程能力仍需要回原课程写代码、运行真实测试并独立复现。
